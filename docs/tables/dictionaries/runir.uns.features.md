# Dictionary: classifier `features.*`

Used by: [`../runir.uns.prove_classifier.md`](../runir.uns.prove_classifier.md).

| Column | Meaning |
|---|---|
| `id` | Feature alias, rendered as `fK`. |
| `symbol` | Classifier feature symbol. |

All classifier features are boolean. Rows are ordered and define the `f0`, `f1`, ... feature columns in classifier `[states]` tables.
