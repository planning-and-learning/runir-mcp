# Dictionary: termination `memory.*`

Used by: [`../runir.ps.ext.prove_termination.md`](../runir.ps.ext.prove_termination.md).

| Column | Meaning |
|---|---|
| `id` | Memory-state alias, rendered as `mK`. |
| `memory` | Memory-state name. |

Termination vertices use this alias in the `[vertices]` `mem` column.
