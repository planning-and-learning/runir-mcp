# Dictionary: classifier `atoms.*`

Used by: [`../runir.uns.prove_classifier.md`](../runir.uns.prove_classifier.md).

| Column | Meaning |
|---|---|
| `id` | Atom alias, rendered as `pK`. |
| `kind` | Atom kind; currently `fluent`. |
| `atom` | Ground atom text. |

Classifier witnesses use `[facts]` to list the misclassified state's fluent atoms as comma-separated `pK` aliases.
