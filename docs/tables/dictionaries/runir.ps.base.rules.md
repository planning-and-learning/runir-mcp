# Dictionary: base `rules.*`

Used by: [`../runir.ps.base.counterexamples.md`](../runir.ps.base.counterexamples.md).

| Column | Meaning |
|---|---|
| `id` | Rule alias, rendered as `rK`. |
| `symbol` | Base sketch-policy rule symbol. |

Rule aliases appear in `[transitions]` and `[successors]` `rule_id` columns. An empty `rule_id` cell means no sketch rule selects that move.
